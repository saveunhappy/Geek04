# 方式一：OpenAI 官方
OPENAI_API_KEY=sk-your-openai-key

# 方式二：MiniMax（pi-mono 内置 provider：minimax-cn，使用 Anthropic 协议）
# 不需要 OPENAI_BASE_URL，直接配置：
# MINIMAX_CN_API_KEY=your-minimax-key

# 方式三：其他 OpenAI 兼容国内模型（DeepSeek/Kimi 等）
# 推荐通过 ~/.pi/agent/models.json 覆盖 baseUrl 和模型，
# 或在代码里直接构造 Model 对象。
# 注意：pi-mono 不会读取 OPENAI_BASE_URL 环境变量。
